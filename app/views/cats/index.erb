<div class="row">
  <div class="col-10">
    <span class="align-bottom">
      <h1>Alle Kazen</h1>
    </span>
  </div>
  <div class="col-2">
    <span class="align-bottom">
      <%= link_to "Neue Katze", new_cat_path, class: "btn btn-primary" %>
    </span>
  </div>
</div>

<div class="row">
  <div class="col-12">
    <br/>
  </div>
</div>

<div class="row">
  <div class="col-12">
    <table class="table table-striped">
      <thead class="bg-primary text-white font-weight-bold">
        <tr>
          <td>
            Vorname
          </td>
          <td>
            Name
          </td>
          <td>
            Alter
          </td>
          <td>
            Info
          </td>
          <td class="text-right">
            Aktionen
          </td>
        </tr>
      </thad>
      <tbody>
        <% @cats.each do |cat| %>
          <tr>
            <td>
              <%= cat.pre_name %>
            </td>
            <td>
              <%= cat.name %>
            </td>
            <td>
              <%= cat.age %>
            </td>
            <td>
              <%= cat.info %>
            </td>
            <td class="text-right">
              <div class="row">
                <div class="col">
                  <%= link_to "Delete", cat_path(cat), method: :delete, data: {confirm: "Bisch sicher ?"}, class: "btn btn-danger" %>
                  <%= link_to "Edit", edit_cat_path(cat), class: "btn btn-primary" %>
                  <%= link_to "View", cat_path(cat), class: "btn btn-primary" %>
                </div>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
